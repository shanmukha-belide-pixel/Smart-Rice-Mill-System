services:
  - type: web
    name: smart-rice-mill-backend
    runtime: docker
    plan: free
    envVars:
      - key: JWT_SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        value: sqlite:///./ricemill.db
